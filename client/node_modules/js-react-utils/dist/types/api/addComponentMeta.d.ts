import { ComponentType } from 'react';
export default addComponentMeta;
declare function addComponentMeta<P>(component: ComponentType<P>, params: {
    name: string;
    validation?: ((props: P) => boolean | null | Error) | null | false;
}): void;
